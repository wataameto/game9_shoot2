# 開発・改善レポート

本プロジェクト（NEON STARFIGHTER 3D）において、プレイヤーの快適なゲーム体験、直感的な操作性、およびボス戦でのバグや不自然な挙動を解消するために実施したすべての改善項目と技術的解決策をまとめました。

---

## 1. プレイヤー耐久力（スタミナ）と生存性の強化
「すぐ死ぬ」という課題に対し、ゲームバランスと安全対策を大幅に強化しました。

* **シールド容量の倍増**：最大シールド（HP）を `100 ➔ 200` に倍増。
* **回復アイテムの強化**：シールド回復量を `35 ➔ 70` に引き上げ。
* **無敵時間（1.2秒）と被弾点滅**：被弾した瞬間に1.2秒間の無敵時間を設け、その間は自機が高速点滅するようにして、連続被弾による一瞬の即死（スタミナ切れ）を防止しました。
* **カメラ復帰テレポートバグの修正**：被弾時のカメラシェイク（揺れ）が終わった瞬間、カメラの現在地を無視して初期座標 `(0, 0, 15)` に強制ワープしてしまい操作が困難になるバグを修正し、シェイク前の正しい現在座標を維持するようにしました。

---

## 2. ボス戦カメラワーク ＆ 誘導HUDの実装
「ラスボスが見つからない」「距離インジケーターは動くのに姿が見えない」という問題をカメラワークとUIの両面から解決しました。

* **ボス注視カメラ (Boss-Targeting Camera) の実装**：
  ボスが出現している間、カメラの注視方向（`lookTarget`）を「自機の前方」と「ボスの現在地」を `35% : 65%` でブレンドした位置に動的に向けるようにしました。ボスが頭上や左右を飛び回っても、カメラが自動的に首を振ってボスを画面内に捉え続けます。
* **ド派手な出現警告 (SYSTEM WARNING)**：
  ボス出現時に、画面全体が赤くサイレン点滅し、中央に「⚠ SYSTEM WARNING ⚠ NEON GOLIATH DETECTED」と流れる大迫力の演出を追加。
* **3DターゲットロックオンHUD (TARGET LOCKED)**：
  ボスの3D位置を画面（2D座標）へ射影し、ボスに追従するロックオン枠を表示。ボスが画面外にいるときは、自動で画面端に警告矢印と直線距離が表示されてプレイヤーを誘導します。
* **レーダーの範囲外ボス警告**：
  ボスがレーダー範囲（270m）の外にいるとき、レーダーの境界線上（最外周）に赤い警告矢印（▽）と距離テキストを点滅表示します（クリッピング処理で矢印が半分削れていたバグも完全修正）。

---

## 3. ボスの行動アルゴリズムの調整（正面維持と並走）
「自機が横を向くとボスが紐でつながれたようについてくる」「2Dモードでボスに近づけない」という問題を、物理と座標計算の修正で解決しました。

* **プレイヤー旋回角度（Yaw）の結合解除**：
  ボスの軌道計算からプレイヤーの回転角度（`yaw`）を完全に排除しました。これにより、プレイヤーが旋回したとき、ボスは世界座標に留まり、画面の横へ自然にスライドして流れるようになりました。
* **プレイヤー移動との並走（Displacement加算）**：
  プレイヤーの前進や横移動の移動量（このフレームでの実際の位置の変化量）を、ボスの位置にも毎フレームダイレクトに加算するようにしました。プレイヤーが全速力で進んでもボスが置き去りにされず、常に追従して並走します。
* **境界衝突時の逃亡バグ修正**：
  プレイヤーが画面の境界（Z=-250）にぶつかって停止しているとき、以前は「キーボードの入力速度」をボスに加算していたため、プレイヤーが止まっているのにボスだけが前方に逃げ続けていました。これを「実際の位置の変化量（停止時は0）」に変えたことで、境界でもボスが逃げずにプレイヤーの正面で待つようになりました。
* **2Dモードでの軌道の最適化（真北固定スイング）**：
  真上から見下ろす2Dモード（カメラ視野がZ方向に狭い）では、360度回ると画面下端からはみ出すため、プレイヤーの「常に真北（Z = -18）」の位置を中心に、左右に `12m` の幅でスイングする軌道に制限しました。視野内に常に収まりつつ、旋回時は自然に移動します。

---

## 4. レーダー（RADAR）の使いやすさ向上
「小物のレーダー表示が大きすぎる」「 Heading-upが分かりにくい」という課題を整理しました。

* **North-up（世界固定・自機回転）への変更**：
  メイン画面が「北が上（世界固定）」でスクロールするのに対し、レーダーが回転すると方向感覚が狂うため、レーダーも **North-up（北が上）** に変更しました。自分が右を向くと、中心の自機マークが右（東）に回転し、敵のいる方角と動きが画面と100%シンクロするようになりました。
* **ドットサイズと速度テールの最適化**：
  敵機（赤ドット）の半径を `3.5 ➔ 1.5`、アイテム（青ドット）の半径を `3.2 ➔ 1.2` にそれぞれ大幅に縮小し、速度ライン（テール）も半分にして画面をシャープに整理しました。
* **敵ドローンと隕石の完全な識別化**：
  一般の敵のうち、隕石は **「グレーのドット（テールなし）」**、敵ドローンは **「ネオンレッドのドット（テールあり）」** で描画し、一目で危険度が分かるようにしました。

---

## 5. 3D描画 ＆ UIインタラクションのバグ修正
ゲームプレイ中の細かなストレスを取り除き、クオリティを上げました。

* **3Dレーザー射撃の仰角補正（3Dエイム）**：
  3Dモードでボスが空中に浮いている（Y=4〜9）のに対し、レーザーが水平（Y=0）にしか飛ばず、ボスの下をすり抜けてダメージが与えられなかったバグを修正。ボス出現中は、レーザーが自動的にボスの3Dコアを直接狙って、斜め上に仰角を付けて発射されるようにしました。
* **左翼の透明化バグの修正 (THREE.DoubleSide)**：
  左翼ポリゴンの3D頂点インデックスの向きが時計回りになっており、裏面カリングによって完全に透明になっていたバグを修正。翼のマテリアルを `THREE.DoubleSide`（両面レンダリング）にし、両翼が正しく表示されるようにしました。
* **TITLEボタンの無反応バグの修正**：
  3Dドラッグ操縦中に、マウスがわずかにブレることでブラウザがクリックイベントをキャンセルしてしまう問題を解決するため、ボタンに `pointerdown` イベントを追加し、押した瞬間に即座にタイトル画面に戻れるようにしました。また、`.ui-layer` の pointer-events 透過バグもクラス付与で解消しました。
* **2D開始時の一瞬のカメラ・自機反転バグの修正**：
  2Dモードの開始時に、ほぼ垂直見下ろしアングルでの `camera.lookAt` 特異点の挙動により、カメラが180度ロール反転して自機が逆向き（下向き）に見えていた現象を修正。導入中はカメラ角度を `-Math.PI/2`（真下）に完全固定して位置だけを降下させるようにしました。

---

## 6. ゲームルール・デバッグ機能の調整
* **ボス出現キル数の緩和**：ボス出現に必要な敵撃破数を15機から **`8機`** に削減。
* **時間経過での自動出現**：敵を一切倒せなくても、開始後 **`45秒`** が経過すれば自動的にボスが出現するタイムアウトルールを追加。
* **デバッグキー『B』キー**：ゲーム中に `B` キーを押すと、目の前（3Dは140m、2Dは25m）にボスが強制出現し、いつでも確認できるようにしました（出現済みの場合は瞬間移動）。

---

## 7. 修正・関係ファイル
* [game.js](file:///Users/user/work/ai/game9_shoot_guruguru/game.js) (メインロジック、カメラ、物理、数学、マテリアル、レーダー、イベント)
* [index.html](file:///Users/user/work/ai/game9_shoot_guruguru/index.html) (HUD要素、警告オーバーレイ、TITLEボタンの interactive 付与)
* [style.css](file:///Users/user/work/ai/game9_shoot_guruguru/style.css) (ロックオンHUD、警告サイレン演出、ボタンのポインターイベント)
